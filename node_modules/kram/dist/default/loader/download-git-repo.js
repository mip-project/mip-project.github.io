'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.downloadGitRepo = downloadGitRepo;

var _fsExtra = require('fs-extra');

var _fsExtra2 = _interopRequireDefault(_fsExtra);

var _downloadGitRepo = require('download-git-repo');

var _downloadGitRepo2 = _interopRequireDefault(_downloadGitRepo);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * @file download-git-repo.js
 * @author tanglei (tanglei02@baidu.com)
 */

function downloadGitRepo(_ref) {
    var from = _ref.from,
        to = _ref.to,
        tmp = _ref.tmp;

    var tmpDir = tmp || to;

    var promise = new Promise(function (resolve, reject) {
        (0, _downloadGitRepo2.default)(from, tmpDir, { clone: false }, function (err) {
            if (err) {
                reject(err);
            } else if (tmp) {
                _fsExtra2.default.move(tmp, to, { overwrite: true }).then(resolve);
            } else {
                resolve();
            }
        });
    });

    return promise;
}